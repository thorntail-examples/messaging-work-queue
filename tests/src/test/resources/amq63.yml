---
kind: Service
apiVersion: v1
spec:
  ports:
  - port: 5672
    targetPort: 5672
  selector:
    deploymentConfig: "shared-work-queue-broker"
metadata:
  name: "shared-work-queue-broker-amqp"
  labels:
    application: "shared-work-queue"
  annotations:
    description: The broker's AMQP port.
---
kind: Service
apiVersion: v1
spec:
  ports:
  - port: 1883
    targetPort: 1883
  selector:
    deploymentConfig: "shared-work-queue-broker"
metadata:
  name: "shared-work-queue-broker-mqtt"
  labels:
    application: "shared-work-queue"
  annotations:
    description: The broker's MQTT port.
---
kind: Service
apiVersion: v1
spec:
  ports:
  - port: 61613
    targetPort: 61613
  selector:
    deploymentConfig: "shared-work-queue-broker"
metadata:
  name: "shared-work-queue-broker-stomp"
  labels:
    application: "shared-work-queue"
  annotations:
    description: The broker's STOMP port.
---
kind: Service
apiVersion: v1
spec:
  ports:
  - port: 61616
    targetPort: 61616
  selector:
    deploymentConfig: "shared-work-queue-broker"
metadata:
  name: "shared-work-queue-broker-tcp"
  labels:
    application: "shared-work-queue"
  annotations:
    description: The broker's OpenWire port.
    service.alpha.openshift.io/dependencies: '[{"name": "shared-work-queue-broker-amqp",
      "kind": "Service"},{"name": "shared-work-queue-broker-mqtt", "kind": "Service"},{"name":
      "shared-work-queue-broker-stomp", "kind": "Service"}]'
---
kind: Service
apiVersion: v1
spec:
  clusterIP: None
  ports:
  - name: mesh
    port: 61616
  selector:
    deploymentConfig: "shared-work-queue-broker"
metadata:
  name: "shared-work-queue-broker-mesh"
  labels:
    application: "shared-work-queue"
  annotations:
    service.alpha.kubernetes.io/tolerate-unready-endpoints: 'true'
    description: Supports node discovery for mesh formation.
---
kind: DeploymentConfig
apiVersion: v1
metadata:
  name: "shared-work-queue-broker"
  labels:
    application: "shared-work-queue"
spec:
  strategy:
    type: Rolling
    rollingParams:
      maxSurge: 0
  triggers:
  - type: ImageChange
    imageChangeParams:
      automatic: true
      containerNames:
      - "shared-work-queue-broker"
      from:
        kind: ImageStreamTag
        name: jboss-amq-63:1.3
  - type: ConfigChange
  replicas: 1
  selector:
    deploymentConfig: "shared-work-queue-broker"
  template:
    metadata:
      name: "shared-work-queue-broker"
      labels:
        deploymentConfig: "shared-work-queue-broker"
        application: "shared-work-queue"
    spec:
      terminationGracePeriodSeconds: 60
      containers:
      - name: "shared-work-queue-broker"
        image: jboss-amq-63
        imagePullPolicy: Always
        readinessProbe:
          exec:
            command:
            - "/bin/bash"
            - "-c"
            - "/opt/amq/bin/readinessProbe.sh"
        ports:
        - name: jolokia
          containerPort: 8778
          protocol: TCP
        - name: amqp
          containerPort: 5672
          protocol: TCP
        - name: mqtt
          containerPort: 1883
          protocol: TCP
        - name: stomp
          containerPort: 61613
          protocol: TCP
        - name: tcp
          containerPort: 61616
          protocol: TCP
        env:
        - name: AMQ_USER
          value: "shared-work-queue"
        - name: AMQ_PASSWORD
          value: "shared-work-queue"
        - name: AMQ_TRANSPORTS
          value: "amqp"
        - name: AMQ_QUEUES
          value: "requests,responses,worker-status"
        - name: AMQ_TOPICS
          value: ""
        - name: MQ_SERIALIZABLE_PACKAGES
          value: ""
        - name: AMQ_MESH_DISCOVERY_TYPE
          value: "dns"
        - name: AMQ_MESH_SERVICE_NAME
          value: "shared-work-queue-broker-mesh"
        - name: AMQ_MESH_SERVICE_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: AMQ_STORAGE_USAGE_LIMIT
          value: "100 gb"
        - name: AMQ_QUEUE_MEMORY_LIMIT
          value: ""
